pandas>=2.0
numpy>=1.24
scipy>=1.10          # assumption tests (Spearman) in customer_base_audit.py
lifetimes>=0.11      # v0.1 modeling core (BG/NBD, Gamma-Gamma) — unmaintained, migration to pymc-marketing planned
matplotlib>=3.7      # executive brief charts
fastapi>=0.110       # v0.3 agent tool server
uvicorn>=0.29
python-multipart>=0.0.9
